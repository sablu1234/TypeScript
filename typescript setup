#typescript setup profess
1. typscript set up a obossoi node and npm install thakte hobe
  check node install ase kina => node -v
  check npm install ase kina  => npm -v

  #jodi install na thake install dite hobe
